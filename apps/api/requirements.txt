fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pillow==11.1.0
numpy==2.2.2
opencv-python-headless==4.11.0.86
aiofiles==24.1.0
httpx==0.28.1            # talk to the GPU worker (engine.py, video_tasks.py)
# Optional (better quality; needs Python 3.11 + enough disk):
# easyocr==1.7.2
# torch==2.5.1
# torchvision==0.20.1
# simple-lama-inpainting==0.1.2
# Optional — only if you use Cloudflare R2 / S3 for video storage:
# boto3==1.35.0
# LPIPS perceptual metric (optional)
# lpips==0.1.4
